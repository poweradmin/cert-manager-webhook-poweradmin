apiVersion: v1
kind: Service
metadata:
  name: {{ include "poweradmin-webhook.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "poweradmin-webhook.name" . }}
    chart: {{ include "poweradmin-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    app: {{ include "poweradmin-webhook.name" . }}
    release: {{ .Release.Name }}
