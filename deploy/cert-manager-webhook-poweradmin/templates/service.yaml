apiVersion: v1
kind: Service
metadata:
  name: {{ include "cert-manager-webhook-poweradmin.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "cert-manager-webhook-poweradmin.name" . }}
    chart: {{ include "cert-manager-webhook-poweradmin.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    app: {{ include "cert-manager-webhook-poweradmin.name" . }}
    release: {{ .Release.Name }}
